<% if @user.image.attached? %>
  <img src = "<%= (url_for(@user.image)) %>" style="width: 150px; height: 150px;" class="rounded-circle shadow-4" alt="username">
<% else %>
<%= image_tag "blank.png"%>
<% end %>